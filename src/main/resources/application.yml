server:
  port: 8081

spring:
  application:
    name: user-job-management
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://jumble_app_user:test123@jumble.ewhoayr.mongodb.net/jumbledb?retryWrites=true&w=majority&appName=JUMBLE}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    org.springframework.data.mongodb: INFO
    org.mongodb.driver: INFO
