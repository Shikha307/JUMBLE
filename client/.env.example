# ============================================================
# LOCAL DEVELOPMENT — copy this to .env.local (git-ignored)
# ============================================================
# .env.local overrides these defaults at dev time.
# Never commit .env.local to git.

VITE_SWIPE_API_URL=http://localhost:8080
VITE_USER_JOB_API_URL=http://localhost:8081
VITE_ML_OUTPUTS_URL=/ml_outputs
